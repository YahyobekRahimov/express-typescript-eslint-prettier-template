<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>Dashboard</title>
    <%- include('../css/base.ejs') %> <%-
    include('../css/dashboard.ejs') %>
  </head>
  <body>
    <%- include('../components/navbar.ejs') %>

    <div class="container">
      <%- include('../components/sidebar.ejs', { currentPage:
      'dashboard', isAdmin }) %>

      <div class="main-content">
        <div class="welcome-card">
          <h2>Welcome, <%= user.username %>!</h2>
          <p>
            You are logged in as <strong><%= user.role %></strong>
          </p>
        </div>

        <div class="section-title">Features</div>
        <div class="features-grid">
          <div class="feature-card">
            <h3>📊 Analytics</h3>
            <p>View your performance metrics and statistics.</p>
          </div>

          <div class="feature-card">
            <h3>📋 Reports</h3>
            <p>Generate and download detailed reports.</p>
          </div>

          <% if (isAdmin) { %>
          <div class="feature-card">
            <h3>👥 User Management</h3>
            <p>
              Manage staff members, add new users, and update
              credentials.
            </p>
            <a href="/dashboard/users" class="btn">Manage Users</a>
          </div>
          <% } %>
        </div>

        <% if (isAdmin) { %>
        <div class="admin-section">
          <h3>🔐 Admin Controls</h3>
          <p>
            As an administrator, you have access to user management
            features. You can add new staff members, update their
            credentials, and manage system settings.
          </p>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
